#include <stdio.h>
int main(){
    int num;
    printf("ingresa tu numero para ver si es positivo o negativo");
    scanf("%d",&num);
    if (num>0)
    {
        printf("tu numero es positivo ");
    }
    else{
        printf("tu numero es negativo");
    }
    
}